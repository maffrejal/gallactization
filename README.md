Gallactization themed (hybrid UI: animated login, dark HUD pages)
---------------------------------------------------------------
Run:
  python -m venv venv
  venv\Scripts\activate  (Windows)
  pip install Flask Flask-SQLAlchemy
  python init_db.py
  python run.py

Login with: admin / password
